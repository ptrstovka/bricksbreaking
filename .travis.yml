language: java

services:
  - postgresql

before_install:
  - psql -c 'create database travis_ci_test;' -U postgres
  - cp local.properties.travis local.properties

script:
  - gradle install
  - gradle test
